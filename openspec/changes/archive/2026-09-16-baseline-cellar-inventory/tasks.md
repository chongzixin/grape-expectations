## 1. Baseline capture

- [x] 1.1 Document existing cellar-inventory behavior as a spec — no implementation change, feature is already live in production. Verify: `openspec/specs/cellar-inventory/spec.md` exists after archive and matches `js/App.tsx`, `js/utils.ts`, `js/components/AddWineModal.tsx`.
