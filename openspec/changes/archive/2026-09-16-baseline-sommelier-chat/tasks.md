## 1. Baseline capture

- [x] 1.1 Document existing sommelier-chat behavior as a spec — no implementation change, feature is already live in production. Verify: `openspec/specs/sommelier-chat/spec.md` exists after archive and matches `js/App.tsx` and `js/components/GuestPage.tsx`.
