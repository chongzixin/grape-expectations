## 1. Baseline capture

- [x] 1.1 Document existing auth behavior as a spec — no implementation change, feature is already live in production. Verify: `openspec/specs/auth/spec.md` exists after archive and matches `js/AuthPage.tsx` and `supabase/schema.sql`.
