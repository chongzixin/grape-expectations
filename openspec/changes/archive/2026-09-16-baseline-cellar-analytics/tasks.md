## 1. Baseline capture

- [x] 1.1 Document existing cellar-analytics behavior as a spec — no implementation change, feature is already live in production. Verify: `openspec/specs/cellar-analytics/spec.md` exists after archive and matches `js/utils.ts` and `js/components/AnalyticsView.tsx`.
